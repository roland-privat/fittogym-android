<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Bare-bones platform theme for the Activity host. The actual Compose
        Material 3 theme lives in ui/theme/Theme.kt and applies inside the
        Activity body. We use a no-action-bar platform parent that follows
        the system day/night setting; Compose handles colors after that.
    -->
    <style name="Theme.RunTraining" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
