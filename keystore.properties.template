# Release keystore credentials — DO NOT COMMIT.
#
# Copy this file to `keystore.properties` (no `.template` suffix) at the
# repo root and fill in your passwords. The real file is gitignored.
#
# After saving, you can build a signed AAB with:
#     .\gradlew.bat bundleRelease
#
# The output lands in app/build/outputs/bundle/release/app-release.aab .

storeFile=C:\\Users\\rnitsch\\.android\\run-training-release.jks
storePassword=REPLACE_ME
keyAlias=run-training
keyPassword=REPLACE_ME
